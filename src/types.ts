export interface ILadder {
	tail: number;
	head: number;
}

export interface ISnake {
	tail: number;
	head: number;
}

export interface IPlayer {
	name: string;
	curr_position: number;
	color:string
}
